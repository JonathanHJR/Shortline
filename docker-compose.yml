services:
  shortline:
    build: .
    image: shortline:latest
    ports:
      - "8081:8080"
    volumes:
      - shortline_data:/app/data
    environment:
      - DATA_FILE=/app/data/links.json
      - BASE_URL=http://localhost:8081

volumes:
  shortline_data: